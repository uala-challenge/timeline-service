# Default properties file for global environment
router:
  port: '8080'
  name: 'timeline-service'

log:
  level: error
  path: '/timeline-service.log'

aws:
  region: 'us-east-1'

dynamo:
  endpoint: 'http://localstack:4566'

redis:
  host: 'redis'
  port: 6379
  db: 0
  timeout: 5

cases:
  tweets:
    table: 'UalaChallenge'
